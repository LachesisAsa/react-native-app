export const selectAuth = (state) => state.auth;

export const selectAuthEmail = (state) => state.auth.userName;
export const selectUserId = (state) => state.auth.userId;
// export const selectAvatar = (state) => state.auth.avatar;
